# CHANGELOG

紫微斗数 命盤鑑定サイト（hoshiyomilune.com）の変更履歴。

サイトは `index.html` 単一ファイル構成。`testfix/index.html` は検証用のステージング。
本番反映は GitHub Pages（ビルド約80秒／キャッシュは `?v=xxx` で回避）。

---

## 修正⑪ — お知らせの再描画・フォールバック改善＋STORESリンク

- **本番コミット**: `6197759`（2026-08-01 13:15）
- **下書き追記**: `034f828`（`upd-6197759`・下書きのみ）
- **ロールバック先**: `a8a0265`

### 背景

「管理人がお知らせを投稿しても他の端末に反映されない」「STORESボタンを押すと現在のサイトに戻る」の2件を調査。D1・Worker API（`GET /api/public/config`）・ロール別フィルタはいずれも正常で、原因は次の2点だった。

1. `_loadRemoteConfig()` の完了後に `renderOshirase()` が呼ばれておらず、通信完了前にお知らせタブを開くと空のまま残る
2. `si-stores-link` が `href="#"` のままで、URLを設定する処理が存在しなかった

### 変更内容

- `_loadRemoteConfig()` 完了後に `_refreshRemoteConfigUI()` で **お知らせ一覧・プロフィール・LINE・STORES を再描画**（タブを開き直さなくても反映される）
- **存在しない `data/config.json` へのフォールバックを削除**（常に404だった）
- 取得失敗時も **localStorage キャッシュを保持**（既存のお知らせが消えない）。`REMOTE_CONFIG_SYNC_KEY` に同期時刻を記録し、4秒後に1回だけ自動リトライ＋タブ復帰時に5分以上古ければ再取得（古いまま固定されない）
- 取得失敗かつキャッシュ無しのときは「現在お知らせはありません」ではなく **🔄再読み込みボタン**を表示（`_retryRemoteConfig()`）
- `_refreshStoresLink()` を新設し、`profile.sites.stores`（プロフィール内のSTORESボタンと同じ値）を特定商取引法タブのリンクへ反映。未設定時は **href を付けず押せない状態**にし、管理人には設定場所を案内、一般利用者には「準備中」と表示
- `showSection('siteinfo')` でも `_refreshStoresLink()` を実行（ログイン状態の変化に追従）

### 検証結果（本番実測）

- 表示件数: 未ログイン **3件**（全員向けのみ）／鑑定士 **6件**／管理人 **6件**
- 通信完了前にタブを開いた場合: 再描画前 0件 → **完了後 6件**
- 通信失敗時: 失敗前6件 → **失敗後も6件**（消えない）。リトライでエラーフラグ解除
- STORESリンク: `https://momos-magicalshop.stores.jp/` へ `target="_blank"` で遷移
- Console エラー **0件**

命盤・暦・十二宮・認証・データ管理は無変更（主要19関数のコード長が本番と完全一致することを確認）。**修正⑩のベースラインは維持**。

---

## 修正⑩ — 十二宮の解説の重複整理 ★宮レイアウトのベースライン

- **本番コミット**: `fd99617`（2026-07-31 20:38）
- **下書き追記**: `ef9a4f1`（`upd-fd99617`・下書きのみ）
- **ロールバック先**: `8132878`
- **状態**: 正式採用・クローズ済み

### 変更内容

命宮以外の11宮で、同一の鑑定文が1つの宮に二度表示されていた問題を解消した。**既存の鑑定文は削除・書き換えを一切行わず、表示位置の整理のみ**。

1. **主星の宮別解説（`STAR_IN_PALACE`）を1箇所に集約**
   - 📌 アコーディオン内の「■ ○○星が○○宮にあると…」のみに表示
   - 旧「🔮 この宮の読み解き」は空宮のときの対宮参照専用に縮小（`_skipYomitoki = mainStarsHere.length > 0`）

2. **副星の宮別解説（`SUB_STAR_PALACE_CTX`）を1箇所に集約**
   - 📌 内の重複ブロックを削除し、「🌙 副星の影響（宮コンテキストで読む）」のみに表示
   - **この項目のみ命宮にも適用**

3. **命宮以外の見出しを変更**（宮別解説と星そのものの解説を区別するため）
   - `⭐ 主星の輝き方` → `⭐ ○○宮に輝く主星`
   - `✦ ○○ が「○○宮」に座す` → `✦ ○○星`
   - 星そのものの説明（◎ 星の本質／💬 口ぐせ・癖／🧠 思考パターン）を開閉式
     `📖 ○○星の本質を詳しく見る` に格納（文言・順序・スタイルは元のまま）

4. **`starLabel()` を新設** — 火星・鈴星のように「星」で終わる星名に「星」を重ねない

### 命宮の構成（変更禁止）

この順序・文言・レイアウトは修正⑧⑨で確定した完成形。今後の修正で変更しないこと。

```
📌 自己・本質・運命テーマ
   ■ 命宮とは？
   ■ ○○星が命宮にあると…
⭐ ○○星が命宮に座す
   ■ ○○星が持つ本来の力
   📖 ○○星の特徴を詳しく見る
🌙 副星の影響（宮コンテキストで読む）
```

### 命宮以外の11宮の構成

```
📌 ○○（宮の役割）
   ■ ○○宮とは？
   ■ ○○星が○○宮にあると…
⭐ ○○宮に輝く主星
   ✦ ○○星
   📖 ○○星の本質を詳しく見る
🌙 副星の影響（宮コンテキストで読む）
（既存の宮別開閉セクション・🗺 十二支・宮干の詳細・✦ この宮の四化）
```

### 不採用にした案

副星ごとの開閉「📖 ○○そのものの性質を見る」は**不採用**。開閉ボタンが命宮で6個→10個に増えるため。副星の説明は従来どおり本文にそのまま表示する。

### 検証結果

- 5命盤 × 12宮＝**60宮**を本番でDOM実測 → 主星・副星とも**重複0件**
- Console エラー **0件**

---

## 修正⑧⑨ — 旧暦変換の全面書き換え＋統合鑑定文＋コントラスト改善

- **本番コミット**: `232c007`（2026-07-31）
- **下書き追記**: `8132878`（`upd-232c007`）
- **ロールバック先**: `2762555`

### 1. 旧暦変換（冬至基準の正式アルゴリズム）

`solarToLunar()` を全面書き換え。新規関数: `sunLongitude()`／`dayNumToJD()`／`hasMajorTerm()`／`winterSolsticeDayNum()`／`newMoonDayNumOnOrBefore()`／`buildLunarMonths()`。

採用した規則:
- 冬至を含む月＝11月
- 次の11月まで13か月なら閏年
- 中気（黄経30°の倍数）を持たない最初の月＝閏月
- 正月＝11月＋2

**注意**: 日→ユリウス日の変換は `n - 0.5 - 8/24`（中国標準時 UTC+8）。`+8/24` にすると2020年の閏4月が外れる。

修正したバグ:
- 旧暦が「13月」と表示される（`cny` を再代入後に再判定していた）
- 閏月が未対応（閏月以降の旧暦月が1つずれる）
- 中気判定の時差誤り
- 旧正月判定の曖昧さ（該当期間に朔が2回ある年で誤判定・1985年等）

検証（1930〜2035年）: 全38,716日で範囲外0件／全1,311か月がすべて29〜30日／閏年・旧正月（15/15）・閏月開始（18/18）・年末年始（8/8）が実暦と一致。

⚠️ **閏月生まれ・旧正月前生まれの方は、この修正で命盤の結果が変わる。**

### 2. 統合鑑定文

`MEI_STAR_READING`（14主星・371〜440字・4段落）を新規追加。**命宮限定**（`hasMeiReading()` が `palIdx===0` で判定）。

星ごとの比喩: 紫微=玉座／天機=歯車／太陽=光／武曲=鋼／天同=湧き水／廉貞=炎／天府=蔵／太陰=月／貪狼=花／巨門=言葉／天相=印璽／天梁=大樹／七殺=刃／破軍=開拓の斧

既存の x.sip ／ ◎ 星の本質 ／ 💬 口ぐせ・癖 ／ 🧠 思考パターン は削除せず、開閉「📖 ○○星の特徴を詳しく見る」内に保持。

### 3. その他

- `pData.theme` の130字カット（`substring(0,130)+'……'`）を解除
- 文字色を `#6d28d9` に変更しコントラスト 1.4:1 → **6.73:1**（WCAG AA 達成）
- ティール `#0d9488` は罫線・アイコン・薄背景の4箇所のみのアクセントに限定

---

## 修正⑥⑦ — 端末内データの利用者別分離＋読込ガード

- **本番コミット**: `2762555`（2026-07-28）／**ロールバック先**: `3a1913f`

- 保存キーを利用者別に分離（`ziwei_saved_charts::guest|admin|kt-0007`）。旧キーは読み書きせず残す
- 管理人のみ初回1回だけ旧データのコピー移行案内（コピーのみ・旧データは削除しない）
- バックアップJSONに `scope`／`scopeName` を記録し、**不一致は確認なしで拒否**
- セッション復元完了前は読込を中止
- ログアウト時に `clearPersonalInputs()` で入力欄と `currentResult` を初期化（保存データは消さない）
- バックアップUIをアコーディオン化（初期閉／成功時は約2.6秒後に自動折りたたみ）

⚠️ `clearPersonalInputs` で `today-unsei-section` の中身ごと消すと再計算でエラーになる。入れ物のdivは残し `today-unsei-body` だけ空にすること。

---

## 修正⑤ — データ管理（JSONバックアップ／復元）

- **本番コミット**: `3a1913f`（2026-07-28）／**ロールバック先**: `569a464`

「📂 保存したデータ」内に「💾 データ管理」を追加。対象キーは `ziwei_saved_charts`／`ziwei_karte_list`／`ziwei_kt_schedule`（＋管理人のみ `ziwei_oshirase_draft`）。権限別スコープ。復元は「追加（おすすめ）」既定と「入れ替え」（2段階確認＋自動バックアップ失敗時は中止）。認証情報の混入は `dbkSafetyCheck` で自己検査。

---

## 修正④ — サイト更新の下書きワークフロー

- **本番コミット**: `569a464`（2026-07-28）／**ロールバック先**: `072d6fa`

下書きは `SITE_UPDATE_DRAFTS`（HTML内の配列）→ `syncSiteUpdateDrafts()` → `localStorage: ziwei_oshirase_draft`。**本番反映のたびに1件追記する運用**（自動生成の仕組みはない）。公開・破棄したidは `ziwei_oshirase_draft_done` に記録し復活を防ぐ。手動投稿（`adminAddOshirase`）は別系統。

**公開ボタンはももさんが押す。Claudeは押さない。テスト投稿も作らない。**

下書き化する: 利用者が気づく新機能・不具合修正・表示/操作性改善・重要な仕様変更
下書き化しない: testfixのみの変更・診断コード・コメント修正・内部整理・同一内容の再コミット

---

## 修正③ — プロフィール画像の自動圧縮＋スマホレイアウト

- **本番コミット**: `072d6fa`（2026-07-27）／**ロールバック先**: `3e03010`

- `onProfileImgChange` で送信サイズ基準の段階圧縮（1000/0.85 → … → 400/0.45、base64が45KB以下になった段で確定）
- **Workerの受信上限は 65,536バイト＝64KB**（`worker/src/index.js` の `parseJson` が Content-Length で判定）。D1の2MBではなくここが実際の制約
- プロフィールカードを flex → grid にし、`.pf-bio` をCSSでスマホのみ全幅化。画像を `grid-row:1/3` で2行にまたがせるのが要点
- grid の配置指定をインラインstyleにするとメディアクエリが効かないため、CSS側に置く

---

## 修正② — スマホ画像保存の2タップ方式

- **本番コミット**: `3e03010`（2026-07-26）／**ロールバック先**: `25ae477`

`downloadMeiban` のcanvas完成後を `_deliverMeibanImage` に分岐。PCは無変更、スマホはプレビュー → タップ2で即share → 4段フォールバック（閉時にBlob URL解放）。

⚠️ WebKitはタップ直後しか share を許可しない（旧方式は NotAllowedError・iPhone Chrome実機で確認）。

---

## 修正① — セッション失効時のUI同期

- **本番コミット**: `25ae477`（2026-07-25）／**ロールバック先**: `289b742`

`_watchSessionUI()`（30秒インターバル＋visibilitychange）でセッション失効を検知し表示のみ更新。根本原因は非同期セッション復元後に `_lastUIRole` が 'public' のまま残る競合で、`updateRoleBadge()` 内で `_lastUIRole=role` を同期する行が本体。追加25行のみ。

---

## 命盤計算の流派判断

### 天馬星（2026-07-31 確定・変更不要）

`TENMA_TABLE=[2,11,8,5,...]` を**年支のみ**で引く。申子辰→寅／亥卯未→巳／寅午戌→申／巳酉丑→亥（シート5「年支系主星算出表」準拠）。

検証: 1984/04/25 01:52 → 甲子年・命宮卯 → 天馬は寅＝**兄弟宮**。

WINEXでは奴僕宮（申＝寅の対宮）と出るが、他サイト複数と照合した結果、兄弟宮が正しいと確認。**WINEXは別流派として扱い、以後の照合基準にしない。**

---

## 開発上の注意

- **Worker／D1／認証／管理人ログイン／鑑定士ログインは変更しない**
- 既存のlocalStorageデータを削除しない・自動移行しない
- 一時的な診断コード（console.log・テスト用フラグ・検証データ）は本番へ含めない
- 検証時、UIセッションは `sessionStorage: ziwei_session_v2`、トークンは `ziwei_at`(session)／`ziwei_rt`(local)。**リロードするとHttpOnly Cookie経由で自動再ログインするため、失効テストは「リロードせず30秒待つ」**
- GitHub UIのコミットボタンはクリックが効かないことがある。その場合はページ内スクリプトで `button[type=submit]` を `.click()` すると通る
