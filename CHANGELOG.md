# CHANGELOG

紫微斗数 命盤鑑定サイト（hoshiyomilune.com）の変更履歴。

サイトは `index.html` 単一ファイル構成。`testfix/index.html` は検証用のステージング。
本番反映は GitHub Pages（ビルド約80秒／キャッシュは `?v=xxx` で回避）。

---

## 修正⑭ — 統合鑑定文を残り7宮へ展開（全12宮完成）★現在のベースライン

- **本番コミット**: `722e649`（2026-08-03 11:22）
- **下書き追記**: `upd-722e649`（下書きのみ）
- **ロールバック先**: `0e8f367`

### 追加した7宮

| 宮index | 宮 | 統合鑑定文の見出し | 宮専用3項目 |
|---|---|---|---|
| 1 | 兄弟宮 | ○○星が仲間とのつながりにもたらすもの | 🤝 仲間との間で出やすい癖／✨ 人脈が広がるときの動き方／⚠ 横の関係で陥りやすい思考 |
| 3 | 子女宮 | ○○星が育てるものにもたらすもの | 🌱 育てるときに出やすい癖／🎓 相手が伸びる関わり方／⚠ 育成で陥りやすい思考 |
| 5 | 疾厄宮 | ○○星が心と体にもたらすもの | 🩺 不調が出やすいところ／💚 体が整う過ごし方／⚠ 健康で陥りやすい思考 |
| 7 | 奴僕宮 | ○○星が協力者との関係にもたらすもの | 🤲 人に頼むときの癖／🌟 協力者が力を出す関わり方／⚠ 人を使うときに陥りやすい思考 |
| 9 | 田宅宮 | ○○星が住まいと家庭にもたらすもの | 🏠 住まい選びに出やすい癖／💎 暮らしが整う住み方／⚠ 家のことで陥りやすい思考 |
| 10 | 福徳宮 | ○○星が心の豊かさにもたらすもの | 🎨 心が求めるもの／🧘 内側が満ちる過ごし方／⚠ 心が疲れるときの思考 |
| 11 | 父母宮 | ○○星が目上との縁にもたらすもの | 👤 目上との間で出やすい癖／🌿 引き立てを受けやすい姿勢／⚠ 目上との関係で陥りやすい思考 |

### 完成した規模

- 統合鑑定文 **168本**（命宮14＋11宮×14星154）
- 宮専用3項目 **462本**（11宮×14星×3）
- **12宮すべてに鑑定文が揃った状態**

### 検証結果（全12宮横断・168本）

本文の完全重複0／冒頭14字の重複0／締め18字の重複0／**20字以上の共通表現0**／宮専用3項目462本の重複0／定型句0／外来文字なし／文字数301〜440字。

品質検査（機械＋目視）: 全168本で段落数4・各段落が句点で終わる・星名が本文にある・宮テーマ語がある・**別宮名の混入0件**。DOM実測で154の宮×星すべてを描画し、**全段落と全項目が正しい宮に表示され、他宮の文章の混入0件**。

幅390pxでの表示崩れ0件。2主星・空宮22ケースでNG0件。Console エラー0件。

既存機能は本番とハッシュ一致で無変更を確認: `MEI_STAR_READING` ccf8382d／`STAR_IN_PALACE` 7f24d3f8／`STAR_PANEL_DATA` 719c2a09／`SUB_STAR_PALACE_CTX` 7dfd220e／`SUB_STAR_DATA` 23032143／`YOMI_DICT` 72be5603／`ENC_STAR` 85857440。命盤計算・暦・認証・保存機能の主要23関数もコード長が完全一致。

### 執筆時に守ったルール（修正⑫から継続）

星ごとの比喩を維持／冒頭の入り方を14星すべて変える／締めくくりも全星異なる／定型句を使わない／300〜450字・4段落。**作成のたびに既存宮との20字重複を自動検査し、見つかった箇所（兄弟宮7・子女宮4・奴僕宮3・福徳宮1）はすべて書き直した。**

---

## 修正⑬ — 読み仮名を Excel マスターへ一本化

- **本番コミット**: `26105b0`（2026-08-03 10:07）
- **下書き追記**: `upd-26105b0`（下書きのみ）
- **ロールバック先**: `2c46499`

### 情報源（唯一のマスター）

`読み仮名マスター_入力用.xlsx`（ももさん入力・全116件／9シート）。**このExcelが読み仮名の唯一の正**。元の資料Excel・一般的な読み方・既存サイトの表記より優先する。

- ①14主星14／②副星28／③十二宮13／④四化星8／⑤五行局6／⑥運気用語9／⑦桃花星7／⑧十二支12／⑨その他専門用語19
- 空欄の項目は登録しない＝**推測で補完しない**

### 変更内容

- `YOMI_DICT`（116件・漢字キー）を新設。同じ読みの別名称（天機／天貴／天喜＝すべて「てんき」）も漢字キーのため混同しない
- ヘルパー: `yomiOf(name)`（未登録は null）／`withYomiSmall(name)`（未登録は空文字＝空括弧・undefinedを出さない）／`applyYomiLabels()`（`data-yomi` 属性から付与）
- `ENC_STAR` の `yomi`（39件）を辞書と同期
- TOPカード4箇所を `data-yomi` 属性化、十二宮モーダル12箇所を `withYomiSmall()` 参照化

### 確定した読みの変更（7件）

武曲 ぶきょく→**ぶごく**／三台 さんたい→**さんだい**／台輔 だいほ→**たいほ**／兄弟宮 きょうだいきゅう→**けいていきゅう**／奴僕宮 どぼくきゅう→**ぬぼくきゅう**／流年 りゅうねん→**るねん**／流月 りゅうげつ→**るげつ**

### あわせて直った既存の誤記（4件）

桃花星 たかばな**ぼし**→とうかせい／四化星 しか**ほし**→しかせい／五行局 ごぎょう**く**→ごぎょうきょく／大限（運気画面）**たいけん**→だいげん

### 現状維持とした項目

大限=だいげん（元資料の「たいげん」は不採用）／火六局=かろっきょく（促音あり）／十二支=訓読み（ね・うし…）／天使=てんし

### 積み残し（別修正で対応）

**辞書対象語の「漢字（かな）」ベタ書きが80箇所／50語残っている**（四化星4・大限4・太歳4・五行局4・化禄3 ほか）。今回はすべての読みをExcelの確定値へ機械的に同期し、構造の置き換えは影響の小さい3系統に留めた。**表示される読みはすべてExcelと一致している**が、辞書参照へ完全に寄せるには `buildPalaceHTML` を含む描画処理に広く手が入るため別段階とする。

### 今後 読みを直すとき

Excelを修正 → `YOMI_DICT` を再生成 → `ENC_STAR` の `yomi` を同期 → 残るベタ書き80箇所も同じ値へ置換、の順で行う。

---

## 修正⑫ — 統合鑑定文を4宮へ展開（夫妻・財帛・官禄・遷移）

- **本番コミット**: `a23f290`（2026-08-01 17:01）
- **下書き追記**: `upd-a23f290`（下書きのみ）
- **ロールバック先**: `10e7786`

### 変更内容

命宮のみだった統合鑑定文を、4宮へ展開した。**命宮のレイアウトと既存の鑑定文は一切変更していない。**

- `PALACE_STAR_READING[宮index][星名]` — 4宮 × 14主星 = **56本**（301〜440字・4段落）
- `PALACE_STAR_DETAIL[宮index][星名]` — 宮専用の短い3項目 = **168本**
- `PALACE_DETAIL_LABELS[宮index]` — 3項目の見出し
- `PALACE_READING_HEADING[宮index]` — 宮別の見出し表現
- `meiReadingText()` / `palaceStarDetail()` / `meiReadingHeading()` を新設
- `hasMeiReading()` を「文章の有無」で判定する形に変更（旧: `palIdx===0`）

宮index: 夫妻宮=2 財帛宮=4 遷移宮=6 官禄宮=8

### 宮別の見出しと3項目

| 宮 | 統合鑑定文の見出し | 宮専用3項目 |
|---|---|---|
| 命宮 | ○○星が持つ本来の力（変更なし） | （なし） |
| 夫妻宮 | ○○星がパートナーシップにもたらすもの | 💕 恋愛・結婚で出やすい癖／❤️ パートナーとの向き合い方／💭 恋愛で陥りやすい思考 |
| 財帛宮 | ○○星がお金と豊かさにもたらすもの | 💰 お金との付き合い方／💎 豊かさを引き寄せる習慣／⚠ お金で陥りやすい思考 |
| 官禄宮 | ○○星が仕事で発揮する力 | 💼 職場で出やすい癖／🌟 評価されやすい働き方／⚠ 仕事で陥りやすい思考 |
| 遷移宮 | ○○星が外の世界で発揮する力 | 🌏 社会で見せる顔／🤝 人から受ける印象／🚀 チャンスを掴みやすい行動 |

開閉「📖 ○○星の特徴を詳しく見る」の中は **宮専用3項目 → ◎ 星の本質／💬 口ぐせ・癖／🧠 思考パターン** の順。既存3項目は文言・順序・スタイルとも維持。

### 執筆ルール（残り7宮を書くときも踏襲すること）

- 星ごとの比喩を維持（紫微=玉座／天機=歯車／太陽=光／武曲=鋼／天同=湧き水／廉貞=炎／天府=蔵／太陰=月／貪狼=花／巨門=言葉／天相=印璽／天梁=大樹／七殺=刃／破軍=開拓の斧）
- **冒頭の入り方を14星すべて変える**（比喩／情景／問いかけ／ワンシーン／セリフ）。「○○は「××」の星です。○宮に座すと」で全星を始めない
- 締めくくりも14星すべて異なる文言にする
- 定型句を使わない（自分らしく／一歩踏み出す／可能性を広げる／信頼される／成功します／努力が評価）
- 300〜450字・4段落・`\n\n` 区切り
- 生成のたびに外来文字（キリル文字・ハングル）混入を自動検査すること

### 検証結果（5宮横断・70本）

本文の完全重複0／冒頭14字の重複0／締め18字の重複0／**20字以上の共通表現0**／宮専用3項目168本の重複0／定型句0／外来文字なし。

本番との差分は `hasMeiReading`（95→86字）と `buildPalaceHTML`（24,980→25,671字）のみ。既存データ5種はハッシュ一致（`MEI_STAR_READING` ccf8382d ほか）。3命盤×12宮=36通りのうち差分8件はすべて対象4宮で、想定外の差分0件。空宮の表示は本番とハッシュ一致。

---

## 修正⑪ — お知らせの再描画・フォールバック改善＋STORESリンク

- **本番コミット**: `6197759`（2026-08-01 13:15）
- **下書き追記**: `034f828`（`upd-6197759`・下書きのみ）
- **ロールバック先**: `a8a0265`

### 背景

「管理人がお知らせを投稿しても他の端末に反映されない」「STORESボタンを押すと現在のサイトに戻る」の2件を調査。D1・Worker API（`GET /api/public/config`）・ロール別フィルタはいずれも正常で、原因は次の2点だった。

1. `_loadRemoteConfig()` の完了後に `renderOshirase()` が呼ばれておらず、通信完了前にお知らせタブを開くと空のまま残る
2. `si-stores-link` が `href="#"` のままで、URLを設定する処理が存在しなかった

### 変更内容

- `_loadRemoteConfig()` 完了後に `_refreshRemoteConfigUI()` で **お知らせ一覧・プロフィール・LINE・STORES を再描画**（タブを開き直さなくても反映される）
- **存在しない `data/config.json` へのフォールバックを削除**（常に404だった）
- 取得失敗時も **localStorage キャッシュを保持**（既存のお知らせが消えない）。`REMOTE_CONFIG_SYNC_KEY` に同期時刻を記録し、4秒後に1回だけ自動リトライ＋タブ復帰時に5分以上古ければ再取得（古いまま固定されない）
- 取得失敗かつキャッシュ無しのときは「現在お知らせはありません」ではなく **🔄再読み込みボタン**を表示（`_retryRemoteConfig()`）
- `_refreshStoresLink()` を新設し、`profile.sites.stores`（プロフィール内のSTORESボタンと同じ値）を特定商取引法タブのリンクへ反映。未設定時は **href を付けず押せない状態**にし、管理人には設定場所を案内、一般利用者には「準備中」と表示
- `showSection('siteinfo')` でも `_refreshStoresLink()` を実行（ログイン状態の変化に追従）

### 検証結果（本番実測）

- 表示件数: 未ログイン **3件**（全員向けのみ）／鑑定士 **6件**／管理人 **6件**
- 通信完了前にタブを開いた場合: 再描画前 0件 → **完了後 6件**
- 通信失敗時: 失敗前6件 → **失敗後も6件**（消えない）。リトライでエラーフラグ解除
- STORESリンク: `https://momos-magicalshop.stores.jp/` へ `target="_blank"` で遷移
- Console エラー **0件**

命盤・暦・十二宮・認証・データ管理は無変更（主要19関数のコード長が本番と完全一致することを確認）。**修正⑩のベースラインは維持**。

---

## 修正⑩ — 十二宮の解説の重複整理 ★宮レイアウトの基準

- **本番コミット**: `fd99617`（2026-07-31 20:38）
- **下書き追記**: `ef9a4f1`（`upd-fd99617`・下書きのみ）
- **ロールバック先**: `8132878`
- **状態**: 正式採用・クローズ済み

### 変更内容

命宮以外の11宮で、同一の鑑定文が1つの宮に二度表示されていた問題を解消した。**既存の鑑定文は削除・書き換えを一切行わず、表示位置の整理のみ**。

1. **主星の宮別解説（`STAR_IN_PALACE`）を1箇所に集約**
   - 📌 アコーディオン内の「■ ○○星が○○宮にあると…」のみに表示
   - 旧「🔮 この宮の読み解き」は空宮のときの対宮参照専用に縮小（`_skipYomitoki = mainStarsHere.length > 0`）

2. **副星の宮別解説（`SUB_STAR_PALACE_CTX`）を1箇所に集約**
   - 📌 内の重複ブロックを削除し、「🌙 副星の影響（宮コンテキストで読む）」のみに表示
   - **この項目のみ命宮にも適用**

3. **命宮以外の見出しを変更**（宮別解説と星そのものの解説を区別するため）
   - `⭐ 主星の輝き方` → `⭐ ○○宮に輝く主星`
   - `✦ ○○ が「○○宮」に座す` → `✦ ○○星`
   - 星そのものの説明（◎ 星の本質／💬 口ぐせ・癖／🧠 思考パターン）を開閉式
     `📖 ○○星の本質を詳しく見る` に格納（文言・順序・スタイルは元のまま）

4. **`starLabel()` を新設** — 火星・鈴星のように「星」で終わる星名に「星」を重ねない

### 命宮の構成（変更禁止）

この順序・文言・レイアウトは修正⑧⑨で確定した完成形。今後の修正で変更しないこと。

```
📌 自己・本質・運命テーマ
   ■ 命宮とは？
   ■ ○○星が命宮にあると…
⭐ ○○星が命宮に座す
   ■ ○○星が持つ本来の力
   📖 ○○星の特徴を詳しく見る
🌙 副星の影響（宮コンテキストで読む）
```

### 命宮以外の11宮の構成

```
📌 ○○（宮の役割）
   ■ ○○宮とは？
   ■ ○○星が○○宮にあると…
⭐ ○○宮に輝く主星
   ✦ ○○星
   📖 ○○星の本質を詳しく見る
🌙 副星の影響（宮コンテキストで読む）
（既存の宮別開閉セクション・🗺 十二支・宮干の詳細・✦ この宮の四化）
```

### 不採用にした案

副星ごとの開閉「📖 ○○そのものの性質を見る」は**不採用**。開閉ボタンが命宮で6個→10個に増えるため。副星の説明は従来どおり本文にそのまま表示する。

### 検証結果

- 5命盤 × 12宮＝**60宮**を本番でDOM実測 → 主星・副星とも**重複0件**
- Console エラー **0件**

---

## 修正⑧⑨ — 旧暦変換の全面書き換え＋統合鑑定文＋コントラスト改善

- **本番コミット**: `232c007`（2026-07-31）
- **下書き追記**: `8132878`（`upd-232c007`）
- **ロールバック先**: `2762555`

### 1. 旧暦変換（冬至基準の正式アルゴリズム）

`solarToLunar()` を全面書き換え。新規関数: `sunLongitude()`／`dayNumToJD()`／`hasMajorTerm()`／`winterSolsticeDayNum()`／`newMoonDayNumOnOrBefore()`／`buildLunarMonths()`。

採用した規則:
- 冬至を含む月＝11月
- 次の11月まで13か月なら閏年
- 中気（黄経30°の倍数）を持たない最初の月＝閏月
- 正月＝11月＋2

**注意**: 日→ユリウス日の変換は `n - 0.5 - 8/24`（中国標準時 UTC+8）。`+8/24` にすると2020年の閏4月が外れる。

修正したバグ:
- 旧暦が「13月」と表示される（`cny` を再代入後に再判定していた）
- 閏月が未対応（閏月以降の旧暦月が1つずれる）
- 中気判定の時差誤り
- 旧正月判定の曖昧さ（該当期間に朔が2回ある年で誤判定・1985年等）

検証（1930〜2035年）: 全38,716日で範囲外0件／全1,311か月がすべて29〜30日／閏年・旧正月（15/15）・閏月開始（18/18）・年末年始（8/8）が実暦と一致。

⚠️ **閏月生まれ・旧正月前生まれの方は、この修正で命盤の結果が変わる。**

### 2. 統合鑑定文

`MEI_STAR_READING`（14主星・371〜440字・4段落）を新規追加。**命宮限定**（`hasMeiReading()` が `palIdx===0` で判定）。

星ごとの比喩: 紫微=玉座／天機=歯車／太陽=光／武曲=鋼／天同=湧き水／廉貞=炎／天府=蔵／太陰=月／貪狼=花／巨門=言葉／天相=印璽／天梁=大樹／七殺=刃／破軍=開拓の斧

既存の x.sip ／ ◎ 星の本質 ／ 💬 口ぐせ・癖 ／ 🧠 思考パターン は削除せず、開閉「📖 ○○星の特徴を詳しく見る」内に保持。

### 3. その他

- `pData.theme` の130字カット（`substring(0,130)+'……'`）を解除
- 文字色を `#6d28d9` に変更しコントラスト 1.4:1 → **6.73:1**（WCAG AA 達成）
- ティール `#0d9488` は罫線・アイコン・薄背景の4箇所のみのアクセントに限定

---

## 修正⑥⑦ — 端末内データの利用者別分離＋読込ガード

- **本番コミット**: `2762555`（2026-07-28）／**ロールバック先**: `3a1913f`

- 保存キーを利用者別に分離（`ziwei_saved_charts::guest|admin|kt-0007`）。旧キーは読み書きせず残す
- 管理人のみ初回1回だけ旧データのコピー移行案内（コピーのみ・旧データは削除しない）
- バックアップJSONに `scope`／`scopeName` を記録し、**不一致は確認なしで拒否**
- セッション復元完了前は読込を中止
- ログアウト時に `clearPersonalInputs()` で入力欄と `currentResult` を初期化（保存データは消さない）
- バックアップUIをアコーディオン化（初期閉／成功時は約2.6秒後に自動折りたたみ）

⚠️ `clearPersonalInputs` で `today-unsei-section` の中身ごと消すと再計算でエラーになる。入れ物のdivは残し `today-unsei-body` だけ空にすること。

---

## 修正⑤ — データ管理（JSONバックアップ／復元）

- **本番コミット**: `3a1913f`（2026-07-28）／**ロールバック先**: `569a464`

「📂 保存したデータ」内に「💾 データ管理」を追加。対象キーは `ziwei_saved_charts`／`ziwei_karte_list`／`ziwei_kt_schedule`（＋管理人のみ `ziwei_oshirase_draft`）。権限別スコープ。復元は「追加（おすすめ）」既定と「入れ替え」（2段階確認＋自動バックアップ失敗時は中止）。認証情報の混入は `dbkSafetyCheck` で自己検査。

---

## 修正④ — サイト更新の下書きワークフロー

- **本番コミット**: `569a464`（2026-07-28）／**ロールバック先**: `072d6fa`

下書きは `SITE_UPDATE_DRAFTS`（HTML内の配列）→ `syncSiteUpdateDrafts()` → `localStorage: ziwei_oshirase_draft`。**本番反映のたびに1件追記する運用**（自動生成の仕組みはない）。公開・破棄したidは `ziwei_oshirase_draft_done` に記録し復活を防ぐ。手動投稿（`adminAddOshirase`）は別系統。

**公開ボタンはももさんが押す。Claudeは押さない。テスト投稿も作らない。**

下書き化する: 利用者が気づく新機能・不具合修正・表示/操作性改善・重要な仕様変更
下書き化しない: testfixのみの変更・診断コード・コメント修正・内部整理・同一内容の再コミット

---

## 修正③ — プロフィール画像の自動圧縮＋スマホレイアウト

- **本番コミット**: `072d6fa`（2026-07-27）／**ロールバック先**: `3e03010`

- `onProfileImgChange` で送信サイズ基準の段階圧縮（1000/0.85 → … → 400/0.45、base64が45KB以下になった段で確定）
- **Workerの受信上限は 65,536バイト＝64KB**（`worker/src/index.js` の `parseJson` が Content-Length で判定）。D1の2MBではなくここが実際の制約
- プロフィールカードを flex → grid にし、`.pf-bio` をCSSでスマホのみ全幅化。画像を `grid-row:1/3` で2行にまたがせるのが要点
- grid の配置指定をインラインstyleにするとメディアクエリが効かないため、CSS側に置く

---

## 修正② — スマホ画像保存の2タップ方式

- **本番コミット**: `3e03010`（2026-07-26）／**ロールバック先**: `25ae477`

`downloadMeiban` のcanvas完成後を `_deliverMeibanImage` に分岐。PCは無変更、スマホはプレビュー → タップ2で即share → 4段フォールバック（閉時にBlob URL解放）。

⚠️ WebKitはタップ直後しか share を許可しない（旧方式は NotAllowedError・iPhone Chrome実機で確認）。

---

## 修正① — セッション失効時のUI同期

- **本番コミット**: `25ae477`（2026-07-25）／**ロールバック先**: `289b742`

`_watchSessionUI()`（30秒インターバル＋visibilitychange）でセッション失効を検知し表示のみ更新。根本原因は非同期セッション復元後に `_lastUIRole` が 'public' のまま残る競合で、`updateRoleBadge()` 内で `_lastUIRole=role` を同期する行が本体。追加25行のみ。

---

## 命盤計算の流派判断

### 天馬星（2026-07-31 確定・変更不要）

`TENMA_TABLE=[2,11,8,5,...]` を**年支のみ**で引く。申子辰→寅／亥卯未→巳／寅午戌→申／巳酉丑→亥（シート5「年支系主星算出表」準拠）。

検証: 1984/04/25 01:52 → 甲子年・命宮卯 → 天馬は寅＝**兄弟宮**。

WINEXでは奴僕宮（申＝寅の対宮）と出るが、他サイト複数と照合した結果、兄弟宮が正しいと確認。**WINEXは別流派として扱い、以後の照合基準にしない。**

---

## 開発上の注意

- **Worker／D1／認証／管理人ログイン／鑑定士ログインは変更しない**
- 既存のlocalStorageデータを削除しない・自動移行しない
- 一時的な診断コード（console.log・テスト用フラグ・検証データ）は本番へ含めない
- 検証時、UIセッションは `sessionStorage: ziwei_session_v2`、トークンは `ziwei_at`(session)／`ziwei_rt`(local)。**リロードするとHttpOnly Cookie経由で自動再ログインするため、失効テストは「リロードせず30秒待つ」**
- GitHub UIのコミットボタンはクリックが効かないことがある。その場合はページ内スクリプトで `button[type=submit]` を `.click()` すると通る

---

## 修正⑮ 鑑定士向け専門解説（167本・12宮共通）

- **本番**: `d418d1f`（2026-08-04）／**ロールバック先**: `7f067c4`
- **testfix**: `f8ec5a6`
- 鑑定士・管理人のみ、各宮に「🔐 鑑定士向け専門解説」を初期閉じの開閉式で表示。
  - ① 宮共通の見方（`PALACE_DATA` の desc / theme / tip）
  - ② 星別の専門ポイント（その宮に実配置の主星のうち、資料がある星のみ）
  - ③ 鑑定メモ・補足（疾厄宮のみ医療注意書き）
- 追加データ: `PALACE_PRO_LABEL` / `PALACE_PRO_COMMON` / `PALACE_STAR_PRO`（167本）、`buildProSection()` を全面書き換え。
- **採用区分**: 正式採用166／準正式採用1（疾厄宮×紫微＝星の健康象意から補強）／補助資料・要確認1（疾厄宮×太陽＝非表示）
- **執筆ルール**: 資料原文をマスターに保持し、表示用文章のみ読みやすく整理。資料にない知識は追加しない。宮共通の技法（四化・対宮・三方四正・六吉星・六凶星）は星別に書かず宮共通へ集約。同じ文章を複数宮で使い回さない。結婚・相性・先祖鑑定へ優先する情報は命盤側へ重複表示しない。
- **資料調査**: 18ファイル・158シート・32,566セルを XML から直接抽出して走査（非表示シート0／非表示行222セル／コメント0／数式663）。取りこぼしていた `ziwei_data.xlsx :: 紫微斗数_財帛宮・子女宮・相生法データ一覧`（宮別セクション）と `紫微斗数読み解きかた :: 紫微斗数メモ`（星別長文セル）、`紫微斗数理論～命盤作成方法`（14主星定義）、`先祖鑑定マスター :: 03_主星マスター` を追加採用。
- 無変更を確認: `calculate` / `solarToLunar` / `buildPalaceHTML` / `isKante` / `YOMI_DICT` / `MEI_STAR_READING` / `PALACE_STAR_READING` / `PALACE_STAR_DETAIL` / `PALACE_DATA` / 認証 / 保存機能。無料版DOMは12宮すべて差分0件。

## 修正⑯ 命盤作成ページのアコーディオン順序（641〜768px）

- **本番**: `eeb12ad`（2026-08-04 12:41 JST）／**ロールバック先**: `d418d1f`
- **testfix**: `db8e746`
- **原因**: `#calc .container` が flex になるのは `@media(max-width:768px)` だが、アコーディオン5件の `order` を与えるメディアクエリが `@media(max-width:640px)` だったため、**641〜768px でアコーディオンの order が初期値0**となり、`order:1〜3` の見出し・計算フォーム・保存パネルより前に表示されていた。
- **修正**: アコーディオン順序のメディアクエリを `640px` → `768px` へ変更（1か所）。DOM順序の変更・JavaScript追加はなし。
- **スコープ**: `#calc #acc-basic` / `#acc-unsei` / `#acc-ai` / `#acc-zensei` / `#acc-senzo` / `#line-cta-unified` / `#today-unsei-section` の7 IDのみ。`inp-date` / `form-group` / `form-row` / `calc-form` / `input` / `select` は対象外。
- **影響**: 640px以下・769px以上は本番と完全一致（769px以上は `display:block` のため order 自体が無効）。
- **不採用**: 修正⑰（生年月日の年・月・日3分割セレクト）は、PC・スマホの入力仕様を変えないという判断により取り消し。`<input type="date" id="inp-date">` を維持。

## 修正⑰（不採用）生年月日の3分割セレクト

- testfix で一度実装したが、PC・スマホの入力仕様を変えない方針により**取り消し**。
- `<input type="date" id="inp-date" value="1990-01-15" min="1930-01-01" max="2025-12-31">` を維持。

## 修正⑱ 相性スコアを新100点設計へ（8項目・関係タイプ連動）

- **本番**: `664f070`（2026-08-08 19:56 JST）／**ロールバック先**: `24d52e8`
- **testfix**: `62f195a` → `78ecd37`（TDZ修正）→ `8b70abe`（支合の表示用定義を復元）
- **背景**: 焦点宮（`REL_TYPES.focusPalaceIdx`）の切替は正常だったが、**総合スコアの計算だけが全10タイプ共通**だった。夫妻宮（palIdx=2）と福徳宮（palIdx=10）が固定値でハードコードされており、同一ペアで全タイプ同点になっていた（実測27点）。
- **出典**: `紫微斗数_相性計算ロジック_サイト実装用.xlsx` の `01_総合スコア設定` / `02_宮別チェック配点`（合計ちょうど100）/ `03_主星相性マトリクス` / `05_副星_桃花_凶星加減点` / `09_実装メモ`

### 新しい100点の内訳

| # | 項目 | 配点 | 内容 | タイプ連動 |
|---|---|---|---|---|
| ① | 本質の相性 | 20 | 命宮×命宮 | 共通 |
| ② | 関係性 | 15 | 関係宮×相手の命宮（双方向平均）。主軸/補助へ配分 | **★** |
| ③ | 心の安心感 | 15 | 福徳宮×福徳宮 | 共通 |
| ④ | 人生の方向性 | 10 | 身宮×身宮（**新規**） | 共通 |
| ⑤ | ご縁の重なり | 10 | 主軸関係宮の三方四正の重複主星数（**新規**） | **★** |
| ⑥ | 惹かれやすさ | 10 | 紅鸞・天喜・天姚・咸池 | 共通 |
| ⑦ | 支え合う力 | 10 | 左輔・右弼・天魁・天鉞 | 共通 |
| ⑧ | 向き合うテーマ | 10 | 擎羊・陀羅・火星・鈴星・地空・地劫・天空（残り点） | 共通 |

- 正規化: `norm(v,max) = clamp(0, max, (v+3)/6*max)`。該当星なし（空宮）は `max*0.5`（中立）。
- 2主星: **全組み合わせの平均**（`09_実装メモ`）。旧実装は1つ目の主星のみ使用していた。
- 副星の集計範囲: **命宮＋主軸関係宮＋その三方四正**（`05` の「同宮・三方四正」に準拠）。宮インデックスは `Set` で重複排除。旧実装は全12宮を平坦化していた。
- **六合+5・支合+3は廃止**（資料に根拠なし）。表示カードは維持。
- 四化は加減点に使わず、既存の飛化クロス表示のみ維持（`subStars` に含まれず宮への帰属が異なるため）。

### タイプ別の関係宮配分（`AISEI_REL_ALLOC`）

love=[夫妻15] / divorcedlove=[夫妻10,子女5] / marriage=[夫妻7,財帛4,田宅4] / friend=[兄弟10,遷移5] / boss=[父母10,官禄5] / sub=[奴僕10,子女5] / colleague=[官禄7,奴僕4,遷移4] / parent=[父母7,田宅4,疾厄4] / child=[子女10,田宅5] / reunion=[夫妻10,福徳5]

補助宮はすべて `REL_TYPES` の `crossHint` に列挙された宮に一致させ、資料にない宮は追加していない。

### 相性タイプ（`AISEI_TYPE_RANGES`）

90-100 深いご縁タイプ / 78-89 安定成長タイプ / 62-77 学び合いタイプ / 45-61 調整必要タイプ / 0-44 カルマ課題タイプ。
各タイプに `strong`（強み）/ `issue`（違い・課題）/ `hint`（育てるヒント）を追加。「運命級」「運命の相手」等の断定表現は使用しない。

### 表示

- **タイプ名を最大**（1.35rem）→ 一言キャッチ → 総合スコア → 注意書き → 説明 → 強み/課題/ヒント → 内訳の順。
- `AISEI_SCORE_NOTE`（「相性スコアは、ふたりの関係の『良い・悪い』を決めるものではありません…」）を**全タイプで常時表示**。
- ⑧向き合うテーマは点数を出さず「少なめ／ほどほど／多め」の3区分。**実測4,191組の分布に基づく3区分**（0〜3個=21.3%／4〜6個=59.6%／7個以上=19.1%）。補足文で「相性の良し悪しを示すものではありません」と明記。

### 実測分布（本番3,141件）

平均59.1／最小34／最大89／範囲外0件／90以上0.00%・78-89が3.50%・62-77が34.4%・45-61が57.3%・0-44が4.8%。**タイプで点が変わったペア 99.7%**。

### 復縁

`REL_TYPES.reunion` は保持。ボタンは追加しない（現状維持）。

### 無変更を確認

命盤計算（meiPos/shinPos/五行局/旧暦）・専門解説167本・YOMI_DICT 116件・認証・保存機能（5件保持）・管理画面・修正⑮⑯。

## 修正⑲改 生年月日入力を全端末共通の3分割セレクトへ

- **testfix**: `fe7c281`（769〜1024px限定版）→ **`cb8384a`（全端末共通版・採用）**
- **背景**: `<input type="date">` のカレンダーは端末により年を直接選べず、数十年前の生年月日を入力するのに月送りを何度も繰り返す必要があった。
- **経緯**: 当初は「タブレット幅（769〜1024px）のみ3分割」で実装したが、実機タブレットで切り替わらなかった。iPad の CSS 幅は機種・向きにより 744／768／820／834／1024／1133／1180／1194／1366px と大きく異なり、幅による出し分けは端末網羅が困難と判断。**幅・UA による判定を全廃し、全端末共通の3分割セレクトへ方針変更**した。
- **診断**: 原因調査用に `testfix/vw.html`（画面幅の実測ページ）を一時追加したが、方針確定後に削除済み。本番には含めない。

### 実装

- HTML: `<input type="date" id="inp-date">` は**残したまま**、隣に `select#inp-y` / `#inp-m` / `#inp-d` を併設。
- CSS: `#inp-date{display:none!important}` で常時非表示、`.ymd-row{display:flex}` で常時表示。**メディアクエリによる出し分けは行わない**（`@media(min-width:1025px)` で最大幅420px、`@media(max-width:400px)` で文字・余白の微調整のみ）。
- JS: 新規関数 `ymdInit()` / `ymdSync()` / `ymdFromDate()` ほか。`#inp-date` の `value` セッターを**この要素だけフック**し、保存データ読込・カルテ・ログアウト初期化など既存のどの経路から書き換えられても3セレクトへ自動反映する。
- **既存関数は1行も変更していない**（`calculate` / `solarToLunar` / `loadSavedChart` / `clearPersonalInputs` / `openChartFromKarte` / `saveCurrentChart` / `renderAiseiResult` をハッシュ照合で確認）。

### 仕様

- 内部値は従来どおり `#inp-date` の `"YYYY-MM-DD"` が正。保存データ構造（`dateVal`）は無変更。
- 年の範囲は `min`/`max` 属性に従う（1930〜2025年・96件）。初期値 `1990-01-15`。
- うるう年・月ごとの日数を判定し、存在しない日付は自動調整（例：2024/2/29 → 2023年へ変更で 2023/2/28）。

### 検証

390 / 430 / 768 / 820 / 1024 / 1440px の全幅で3分割・横一列・見切れ0・横スクロール0。タップ領域は高さ44pxを確保。PCは最大420pxで頭打ち。

無変更を確認：命盤計算・旧暦・主星副星配置／相性ロジック（修正⑱）／専門解説167本（修正⑮）／YOMI_DICT 116件（修正⑬）／TOP並び順（修正⑯）／認証・保存・カルテ・管理画面。
